
These are default images to use in fresh installations
(not based on a user-provided config file).

They are saved as tarballs for faster downloading.

Swap partition is 2GB by default, and configured by the script.
Root will be expanded to use all remaining available space.

nixberry-pi5
nixberry-pi5-f
nixberry-pi5-fm
nixberry-pi5-fh
nixberry-pi5-fmh
nixberry-pi5-m
nixberry-pi5-mh
nixberry-pi5-h

f = flakes
m = home manager
h = hyprland


